# THEORY uses pyproject.toml as the canonical dependency source.
#
# Install for use:
#   pip install -e .
#
# Install for development (includes pytest, ruff, pip-audit):
#   pip install -e ".[dev]"
#
# Keeping this file as a pointer so tooling that expects requirements.txt
# (Dependabot ecosystem detection, Read the Docs, some IDE inspectors)
# still finds something at this path.
-e .
