# Dev dependencies are declared in pyproject.toml under [project.optional-dependencies].dev
#
# Install with:
#   pip install -e ".[dev]"
#
# Include the web UI (theory serve):
#   pip install -e ".[dev,serve]"
-e .[dev]
